<manifest package="no.hyper.memoryorm"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <service
            android:name=".service.FetchInBackground"
            android:exported="false"/>

        <service
            android:name=".service.WriteInBackground"
            android:exported="false"/>

    </application>

</manifest>
